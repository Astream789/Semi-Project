{% extends 'base.html.twig' %}

{% block body %}
<div class="bs-example" data-example-id="striped-table">
    <table class="table table-striped">
        <thead><button type="button" class="btn">Basic</button>
        <tr>
            <th>#</th>
            <th>Tag</th>

        </tr>
        </thead>
        <tbody>
        {% for tag in tags %}
            <tr>
                <th scope="row">{{ tag.id }}</th>
                <td><a href="{{path('bookByTag',{'id':tag.id})}} ">{{ tag.Tgname }}</a></td>

                <td>
                    {#                        <a href="{{ path('todo_details', {'id': book.id}) }} " class='btn btn-success'>View</a>#}
                    {#                        <a href="{{ path('todo_edit', {'id': book.id}) }}" class='btn btn-default'>Edit</a>#}
                    {#                        <a href="{{ path('todo_delete', {'id': book.id}) }}" class='btn btn-danger'>Delete</a>#}
                    <a href="{{path('bookByTag',{'id':tag.id})}} " class='btn btn-success'>View</a>
                    <a href="#" class='btn btn-default'>Edit</a>
                    <a href="{{ path('book_delete', {'id': category.id}) }} "class='btn btn-danger'>Delete</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
<div class="bs-example" data-example-id="striped-table">
    <table class="table table-striped">
        <thead>
        <tr>
            <th>#</th>
            <th>Book Name</th>
            <th>Description</th>
            <th>Date</th>
            <th>Image</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for book in books %}
        <tr>
            <th scope="row">{{ book.id }}</th>
            <td>{{ book.BkName }}</td>
            <td>{{ book.Description }}</td>
            <td>{{ book.Date|date('F j,Y, g:i a') }}</td>
            <td> <a href="{{ asset('upload/bookImages/' ~ book.Image) }}">View image</a><td>
            <td>
{#                        <a href="{{ path('todo_details', {'id': book.id}) }} " class='btn btn-success'>View</a>#}
                {#                        <a href="{{ path('todo_edit', {'id': book.id}) }}" class='btn btn-default'>Edit</a>#}
                {#                        <a href="{{ path('todo_delete', {'id': book.id}) }}" class='btn btn-danger'>Delete</a>#}
                <a href="{{path('book_details',{'id':book.id})}} " class='btn btn-success'>View</a>
                <a href="#" class='btn btn-default'>Edit</a>
                <a href="{{ path('book_delete', {'id': book.id}) }} "class='btn btn-danger'>Delete</a>
            </td>
        </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
