{% extends 'base.html.twig' %}
{% block body %}

    <div class="container-fluid mt-3">
        <h3>Tag List</h3>
    </div>
    </thead>
    <div class="bs-example" data-example-id="striped-table">
        <table class="table table-striped">
            <tr>
                <th>#</th>
                <th>Tag</th>

            </tr>
            <tbody>
            {% for tag in tags %}
                <tr>
                    <th scope="row">{{ tag.id }}</th>
                    <td><a href="{{path('bookByTag',{'id':tag.id})}} ">{{ tag.Tgname }}</a></td>

                    <td>
                        {#                        <a href="{{ path('todo_details', {'id': book.id}) }} " class='btn btn-success'>View</a>#}
                        {#                        <a href="{{ path('todo_edit', {'id': book.id}) }}" class='btn btn-default'>Edit</a>#}
                        {#                        <a href="{{ path('todo_delete', {'id': book.id}) }}" class='btn btn-danger'>Delete</a>#}
                        <a href="{{path('bookByTag',{'id':tag.id})}} " class='btn btn-success'>View</a>
                        <a href="#" class='btn btn-default'>Edit</a>
                        <a href="{{ path('book_delete', {'id': tag.id}) }} "class='btn btn-danger'>Delete</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
