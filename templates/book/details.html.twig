{% extends 'base.html.twig' %}

{% block body %}
        <a href="{{ path('book_list') }}" class="btn btn-default">Back to Product List</a>
        <hr/>
        <h2 class="page-header">{{ books.BkName }}</h2>
        <ul class="list-group">
            <li class="list-group-item">Due: <strong>{{ books.Date|date('F j,Y, g:i a') }}</strong></li>
        </ul>
        <p>{{ books.Detail }}</p>
{% endblock %}
